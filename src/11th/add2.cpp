// Program to accept two numbers and add them
#include <iostream>
#include <conio.h>
using namespace std;
int main()
{
    int a, b, c;
    cout << "Enter any two numbers" << endl;
    cin >> a >> b;
    c = a + b;
    cout << "Addition of " << a << " and " << b << " is " << c;
    return 0;
    getch();
}

/** 
 * // Same Program for Turbo C++
 * #include <iostream.h>
 * #include <conio.h>
 * void main()
 * {
 * clrscr();
 * int a,b,c;
 *  cout << "Enter any two numbers" << endl;
 * cin >> a >> b;
 *  c = a + b;
 *  cout << "Addition of " << a << " and " << b << " is " << c;
 * getch();
 * }
*/

/** 
 * 1.) Algorithm
 * 2.) Flowchart
 * 3.) Program
 * 4.) Output
 */
